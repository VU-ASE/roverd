name: a
author: vu-ase
source: github.com/vu-ase/example-service
version: 1.0.0

commands:
  build: make build
  run: ./example-service

inputs:
  # no inputs, this is the first service in the pipeline

outputs:
  - a

configuration:
