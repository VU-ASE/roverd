name: e
author: vu-ase
source: github.com/vu-ase/example-service
version: 1.0.0

commands:
  build: make build
  run: ./example-service

inputs:
  - service: d
    streams:
      - d

outputs:
  # no outputs, this is the last service in the pipeline

configuration:
