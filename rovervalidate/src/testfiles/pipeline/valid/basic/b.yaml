name: controller
author: vu-ase
source: github.com/vu-ase/example-service
version: 1.0.0

commands:
  build: make build
  run: ./example-service

inputs:
  - service: imaging
    streams:
      - picture

outputs:
  # no outputs, this is the last service in the pipeline

configuration:
