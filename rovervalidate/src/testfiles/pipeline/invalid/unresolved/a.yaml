name: a
author: vu-ase
source: github.com/vu-ase/example-service
version: 1.0.0

commands:
  build: make build
  run: ./example-service

inputs:
  - service: x # this service does not exist
    streams:
      - x

outputs:
  - a

configuration:
